{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs-20_x"]
    },
    "install": {
      "cmds": ["npm ci"]
    },
    "build": {
      "cmds": ["echo 'No build step required'"]
    },
    "start": {
      "cmd": "node server.js"
    }
  }
}
